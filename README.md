# third-wave-jukebox
Project started in 2025 to build a music review website from scratch. Will see how it goes


# Music Review Minimal Starter (Flask + JS)

## 1) Setup
```bash
cd backend
python -m venv .venv && source .venv/bin/activate  # Windows: .venv\\Scripts\\activate
pip install -r requirements.txt
cp .env.example .env  # edit if you want a different DB path
